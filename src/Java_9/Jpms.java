package Java_9;

public class Jpms {
    // There is another repository of jpms which covers every thing


    //we can package our java application, java packages into java modules
    //using java module we can specify which of the packages of the module should be visible to other Java modules
    //A Java module must also specify which other Java modules is requires to do its job.
}
